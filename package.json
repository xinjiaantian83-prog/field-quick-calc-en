{
  "name": "field-quick-calc",
  "version": "0.1.0",
  "description": "DIY-focused imperial jobsite calculator prototype",
  "main": "server.js",
  "scripts": {
    "check": "node --check js/app.js && node --check js/rebar-data.js && node --check js/rebar-calculator.js && node --check js/rebar-ui.js",
    "test": "node --test tests/*.test.cjs",
    "build": "npm run check && npm test && node scripts/build-capacitor-web.mjs",
    "start": "node server.js",
    "cap:sync": "npm run build && npx cap sync",
    "cap:sync:ios": "npm run build && npx cap sync ios",
    "cap:sync:android": "npm run build && npx cap sync android",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android",
    "monetization:ids": "node scripts/configure-monetization-ids.mjs",
    "monetization:app-ads": "node scripts/generate-app-ads.mjs"
  },
  "keywords": [
    "diy",
    "construction",
    "calculator",
    "imperial"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor-community/admob": "^8.1.0",
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1",
    "@capgo/native-purchases": "^8.7.0"
  }
}
